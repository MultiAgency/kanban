{
  "name": "kanban-webhook",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that translates GitHub `issues` webhook events into natural-language prompts for an IronClaw agent.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.4.0",
    "wrangler": "^4.92.0"
  }
}
